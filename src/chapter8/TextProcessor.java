package chapter8;

/*public class TextProcessor {
   /** public static void main(String[] args){
       /* countwords("I love Test Automation University");
          reverseString("Hello TAU");
               */

       /* addSpaces("Hola comoestas?")

    }

    /**
     * Splits a String into an array bu tokenizing it.
     * Count words and print them
     * @param text Full string to be split
     */
 /*   public static void countwords(String text){
        var words = text.split();
        int nomberOfWords = words.lenght;

        String message = String.format("Your text contains % words", numberOfWords);
        System.out.println(message);

        for(int i=0; i<nomberOfWords; i++){
            System.out.println(words[i]);
        }

        public static void reverseString(String text){
            for(int i=text.length()-1; i>=0; i--){
                System.out.print(text.charAt(i));
            }
        }

        public static void addSpaces(String text){

            var modifiedText = new StringBuilder(text);

            for(int i=0; i< modifiedText.length(); i++){
                if(i!=0 && Character.isUpperCase(modifiedText.charAt(i))){
                    modifiedText.insert(i, " ");
                    i++;
                }
            }

            System.out.println(modifiedText);
        }


    }
} */

